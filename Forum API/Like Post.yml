info:
  name: Like Post
  type: http
  seq: 12

http:
  method: POST
  url: http://localhost:3000/api/likes/{{postId}}
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5