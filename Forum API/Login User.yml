info:
  name: Login User
  type: http
  seq: 5

http:
  method: POST
  url: "URL: http://localhost:3000/api/auth/login"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
